package it.unibo.tankBattle;

//import static org.junit.jupiter.api.Assertions.*; //importantissimo per generare test

public class Test {

    //Example for future implementations
    //A Factory used to generate number through streams
    
    // private NumberStream factory;

    // @org.junit.jupiter.api.BeforeEach       //da mettere nel momento in cui viene generata la factory su cui effettuare i test
	// public void initFactory() {
	// 	this.factory = new NumberStreamImpl();
	// }
	
	// @org.junit.jupiter.api.Test            //da mettere prima di ogni test
	// public void testEvenNumbers() {        //ogni nome dev'essere indicativo del test eseguito
	// 	var l = this.factory.evenNumbers().limit(4).collect(Collectors.toList());
    //     assertEquals(Arrays.asList(0, 2, 4, 6), l);
	// }

    // @org.junit.jupiter.api.Test
    // public void testSquares() {
    //     var l = this.factory.squares().limit(5).collect(Collectors.toList());
    //     assertEquals(Arrays.asList(0, 1, 4, 9, 16), l);
    // }

}
